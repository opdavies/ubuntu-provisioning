---
- name: Download PhpStorm.
  unarchive:
    src: "https://download.jetbrains.com/webide/PhpStorm-{{ phpstorm_version }}.tar.gz"
    dest: "{{ phpstorm_destination }}"
    copy: no
  register: phpstorm_downloaded

- name: Install Java JDK.
  apt:
    name: openjdk-7-jdk
